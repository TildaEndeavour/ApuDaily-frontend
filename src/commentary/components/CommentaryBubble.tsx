import React from "react";
import type {CommentaryResponseDto} from "../model/dto/CommentaryResponseDto.ts";

const CommentaryBubble: React.FC<{commentary: CommentaryResponseDto}> = ({commentary}) => {
    return (
        <div>
            <section>

            </section>
        </div>
    );
}

export default CommentaryBubble;